meta {
  name: create request
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/procurement-requests
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{Token_RDBI}}
}

body:json {
  {
    "assetName": "PC",
    "quantity": 1,
    "category": "electronics",
    "reason": "Untuk praktikum GIS"
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
